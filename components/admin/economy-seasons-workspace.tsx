"use client"

import { useCallback, useEffect, useMemo, useState, type ReactNode } from "react"
import { BarChart3, CalendarClock, Coins, Download, Gift, Loader2, Plus, RefreshCw, RotateCcw, Settings2, Trophy, X } from "lucide-react"

type Season={id:string;name:string;economy_version:string;status:"planned"|"active"|"closed";starts_at:string;ends_at:string|null;opening_grant:number;minimum_eligible_questions:number;monthly_rewards:number[];seasonal_rewards:number[];member_count:number;currency_supply:string;currency_earned:string}
type Config={version:string;npConfig:Record<string,unknown>;xpConfig:Record<string,unknown>}
type Run={id:string;reward_type:string;period_key:string;eligible_count:number;excluded_count:number;total_np:string;executed_at:string;recipients:Array<{userId:string;place:number;score:string;npAmount:number}>}
type GiftRow={id:string;index_number:string;name:string|null;amount:number;reason:string;batch_id:string|null;created_at:string}
type Data={seasons:Season[];activeSeason:Season|null;dryRunReport:{approved_users:number;ineligible_users:number};config:Config;rewardRuns:Run[];gifts:GiftRow[]}
type Tab="overview"|"seasons"|"rewards"|"rules"|"gifts"
const tabs:Array<[Tab,string,typeof BarChart3]>=[["overview","Overview",BarChart3],["seasons","Seasons",CalendarClock],["rewards","Rewards",Trophy],["rules","NP & XP Rules",Settings2],["gifts","Gift NP",Gift]]
const control="min-h-11 rounded-xl border border-border bg-background px-3 text-sm outline-none focus:ring-2 focus:ring-primary/25"
const button="inline-flex min-h-10 items-center justify-center gap-2 rounded-xl px-4 text-sm font-bold disabled:pointer-events-none disabled:opacity-40"
const card="rounded-2xl border border-border bg-card p-5 shadow-sm"

export function EconomySeasonsWorkspace({canReset}:{canReset:boolean}){
  const [data,setData]=useState<Data|null>(null),[loading,setLoading]=useState(true),[busy,setBusy]=useState(""),[error,setError]=useState(""),[notice,setNotice]=useState(""),[tab,setTab]=useState<Tab>("overview")
  const [creating,setCreating]=useState(false),[editing,setEditing]=useState<Season|null>(null),[activating,setActivating]=useState<Season|null>(null),[impact,setImpact]=useState<Record<string,number>|null>(null),[confirmation,setConfirmation]=useState("")
  const [name,setName]=useState(""),[version,setVersion]=useState(""),[openingGrant,setOpeningGrant]=useState(500),[startsAt,setStartsAt]=useState(""),[minimum,setMinimum]=useState(300),[monthly,setMonthly]=useState([500,300,200,100]),[seasonal,setSeasonal]=useState([3000,2000,1000,250])
  const previousMonth=new Date(Date.UTC(new Date().getUTCFullYear(),new Date().getUTCMonth()-1,1)).toISOString().slice(0,7)
  const [month,setMonth]=useState(previousMonth),[monthPreview,setMonthPreview]=useState<Record<string,unknown>|null>(null),[payoutConfirmation,setPayoutConfirmation]=useState("")
  const [activeMinimum,setActiveMinimum]=useState(300),[activeMonthly,setActiveMonthly]=useState<number[]>([]),[activeSeasonal,setActiveSeasonal]=useState<number[]>([])
  const [npConfig,setNPConfig]=useState<Record<string,unknown>>({}),[xpConfig,setXPConfig]=useState<Record<string,unknown>>({}),[configReason,setConfigReason]=useState(""),[configPreview,setConfigPreview]=useState<Array<{path:string;before:unknown;after:unknown}>>([]),[configConfirmation,setConfigConfirmation]=useState("")
  const [giftText,setGiftText]=useState(""),[giftPreview,setGiftPreview]=useState<Record<string,unknown>|null>(null)
  const load=useCallback(async()=>{setLoading(true);setError("");try{const response=await fetch("/api/admin/economy-seasons",{cache:"no-store"}),body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.error??"Unable to load Economy Seasons.");setData(body);setNPConfig(body.config.npConfig);setXPConfig(body.config.xpConfig)}catch(cause){setError(cause instanceof Error?cause.message:"Unable to load Economy Seasons.")}finally{setLoading(false)}},[])
  useEffect(()=>{void load();const saved=sessionStorage.getItem("mednexus-economy-tab") as Tab|null;if(saved&&tabs.some(([id])=>id===saved))setTab(saved)},[load])
  useEffect(()=>{if(data?.activeSeason){setActiveMinimum(data.activeSeason.minimum_eligible_questions);setActiveMonthly(data.activeSeason.monthly_rewards);setActiveSeasonal(data.activeSeason.seasonal_rewards)}},[data?.activeSeason])
  const choose=(next:Tab)=>{setTab(next);sessionStorage.setItem("mednexus-economy-tab",next)}
  const request=async(body:Record<string,unknown>,key:string)=>{setBusy(key);setError("");setNotice("");try{const response=await fetch("/api/admin/economy-seasons",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)}),result=await response.json().catch(()=>({}));if(!response.ok)throw new Error(result.error??"Economy operation failed.");if(result.seasons)setData(result);return result}catch(cause){setError(cause instanceof Error?cause.message:"Economy operation failed.");return null}finally{setBusy("")}}
  const gifts=useMemo(()=>giftText.split(/\r?\n/).map(line=>line.trim()).filter(Boolean).map(line=>{const [indexNumber,amount,...reason]=line.split(",");return {indexNumber:indexNumber?.trim(),amount:Number(amount),reason:reason.join(",").trim()}}),[giftText])
  if(loading&&!data)return <div role="status" className="grid min-h-64 place-items-center"><Loader2 className="animate-spin text-primary"/><span className="sr-only">Loading Economy Seasons</span></div>
  if(!data)return <div role="alert" className="rounded-2xl border border-destructive/30 bg-destructive/10 p-5 text-sm text-destructive"><p>{error||"Economy Seasons is unavailable."}</p><button onClick={()=>void load()} className={`${button} mt-4 border`}><RefreshCw size={16}/>Retry</button></div>
  const active=data.activeSeason
  return <main className="mx-auto w-full max-w-7xl space-y-5">
    <header className="flex flex-col gap-4 border-b border-border pb-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-primary/10 text-primary"><Coins size={22}/></span><div><p className="text-xs font-bold text-primary">Admin economy</p><h1 className="text-2xl font-bold sm:text-3xl">Economy Seasons</h1></div></div><div className="flex gap-2"><a href="/api/admin/economy-seasons?download=1" className={`${button} border bg-card`}><Download size={15}/>Report</a><button onClick={()=>void load()} className={`${button} border bg-card`}><RefreshCw size={15}/>Refresh</button></div></header>
    <nav aria-label="Economy sections" className="flex gap-1 overflow-x-auto rounded-2xl border bg-card p-1.5">{tabs.map(([id,label,Icon])=><button key={id} onClick={()=>choose(id)} aria-current={tab===id?"page":undefined} className={`flex min-h-10 shrink-0 items-center gap-2 rounded-xl px-3 text-sm font-bold ${tab===id?"bg-primary text-primary-foreground":"text-muted-foreground hover:bg-muted"}`}><Icon size={15}/>{label}</button>)}</nav>
    {error&&<div role="alert" className="rounded-xl border border-destructive/30 bg-destructive/10 p-3 text-sm text-destructive">{error}</div>}{notice&&<div role="status" className="rounded-xl border border-emerald-500/30 bg-emerald-500/10 p-3 text-sm text-emerald-700">{notice}</div>}{!canReset&&<div className="rounded-xl border bg-muted/30 p-3 text-sm text-muted-foreground">Economy changes require super administrator access. This workspace is read-only.</div>}

    {tab==="overview"&&<div className="space-y-5"><section className={card}><div className="flex justify-between gap-3"><div><p className="text-xs font-bold text-emerald-600">Active season</p><h2 className="text-2xl font-bold">{active?.name??"No active season"}</h2><p className="text-sm text-muted-foreground">Economy {active?.economy_version??"—"} · {active?`Started ${date(active.starts_at)}`:"Plan a season to begin"}</p></div>{active&&<span className="h-fit rounded-full bg-emerald-500/10 px-3 py-1 text-xs font-bold text-emerald-600">Active</span>}</div><dl className="mt-6 grid grid-cols-2 gap-4 md:grid-cols-5"><Stat label="Learners" value={data.dryRunReport.approved_users}/><Stat label="NP supply" value={Number(active?.currency_supply??0).toLocaleString()}/><Stat label="NP awarded" value={Number(active?.currency_earned??0).toLocaleString()}/><Stat label="Eligibility" value={`${active?.minimum_eligible_questions??0} questions`}/><Stat label="Next finalization" value={previousMonth}/></dl></section><div className="grid gap-4 md:grid-cols-3"><Summary title="Season management" text={`${data.seasons.filter(item=>item.status==="planned").length} planned · ${data.seasons.filter(item=>item.status==="closed").length} completed`} action={()=>choose("seasons")}/><Summary title="Reward history" text={`${data.rewardRuns.length} finalized runs`} action={()=>choose("rewards")}/><Summary title="Economy rules" text={`Active ${data.config.version}`} action={()=>choose("rules")}/></div></div>}

    {tab==="seasons"&&<div className="space-y-4"><div className="flex items-center justify-between"><div><h2 className="text-xl font-bold">Season lifecycle</h2><p className="text-sm text-muted-foreground">Plan seasons and review past outcomes.</p></div>{canReset&&<button onClick={()=>setCreating(true)} className={`${button} bg-primary text-primary-foreground`}><Plus size={16}/>Plan season</button>}</div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{data.seasons.map(season=><article key={season.id} className={card}><div className="flex justify-between"><div><p className="text-xs text-muted-foreground">Economy {season.economy_version}</p><h3 className="text-lg font-bold">{season.name}</h3></div><span className="h-fit rounded-full bg-muted px-2.5 py-1 text-xs font-bold capitalize">{season.status}</span></div><dl className="mt-5 grid grid-cols-2 gap-4"><Stat label="Opening grant" value={`${season.opening_grant} NP`}/><Stat label="Members" value={season.member_count}/><Stat label="NP supply" value={Number(season.currency_supply).toLocaleString()}/><Stat label={season.status==="closed"?"Closed":"Starts"} value={date(season.ends_at??season.starts_at)}/></dl>{season.status==="planned"&&canReset&&<div className="mt-5 grid grid-cols-2 gap-2"><button onClick={()=>setEditing(season)} className={`${button} border`}>Edit</button><button onClick={async()=>{setActivating(season);setImpact(null);setConfirmation("");const result=await request({action:"preview_activation",seasonId:season.id},"impact");if(result)setImpact(result.impact)}} className={`${button} border border-primary/30 text-primary`}><RotateCcw size={16}/>Start</button></div>}</article>)}</div></div>}

    {tab==="rewards"&&<div className="space-y-5"><div className="grid gap-5 xl:grid-cols-2"><section className={card}><h2 className="text-lg font-bold">Reward rules</h2><p className="text-sm text-muted-foreground">Set eligibility and NP prizes for the active season.</p><Num label="Minimum eligible questions" value={activeMinimum} set={setActiveMinimum}/><div className="mt-4"><RewardTable title="Monthly NP rewards" value={activeMonthly} set={setActiveMonthly}/></div><div className="mt-4"><RewardTable title="Seasonal NP rewards" value={activeSeasonal} set={setActiveSeasonal}/></div>{canReset&&active&&<button disabled={busy==="rules"} onClick={async()=>{if(await request({action:"update_rules",seasonId:active.id,minimumEligibleQuestions:activeMinimum,monthlyRewards:activeMonthly,seasonalRewards:activeSeasonal},"rules"))setNotice("Reward rules saved.")}} className={`${button} mt-5 bg-primary text-primary-foreground`}>Save reward rules</button>}</section><section className={card}><h2 className="text-lg font-bold">Finalize monthly rewards</h2><p className="text-sm text-muted-foreground">Preview leaders and exact NP payouts first.</p><div className="mt-4 flex gap-2"><input type="month" max={previousMonth} value={month} onChange={event=>{setMonth(event.target.value);setMonthPreview(null)}} className={`${control} flex-1`}/><button onClick={async()=>{const result=await request({action:"preview_month",month},"month");if(result)setMonthPreview(result)}} className={`${button} border`}>Preview</button></div>{monthPreview&&<div className="mt-4 rounded-xl bg-muted/40 p-4"><dl className="grid grid-cols-3 gap-3"><Stat label="Eligible" value={Number(monthPreview.eligibleCount??0)}/><Stat label="Excluded" value={Number(monthPreview.excludedCount??0)}/><Stat label="Payout" value={`${Number(monthPreview.totalNP??0).toLocaleString()} NP`}/></dl>{Boolean(monthPreview.alreadyFinalized)?<p className="mt-4 font-bold text-amber-600">Already finalized.</p>:canReset&&<><input value={payoutConfirmation} onChange={event=>setPayoutConfirmation(event.target.value)} placeholder={`Type FINALIZE ${month}`} className={`${control} mt-4 w-full`}/><button disabled={payoutConfirmation!==`FINALIZE ${month}`} onClick={async()=>{if(await request({action:"award_month",month,confirmation:payoutConfirmation},"award")){setMonthPreview(null);setPayoutConfirmation("");setNotice("Monthly NP rewards finalized.")}}} className={`${button} mt-3 w-full bg-primary text-primary-foreground`}>Finalize month</button></>}</div>}</section></div><section className={card}><h2 className="text-lg font-bold">Reward history</h2><div className="mt-4 grid gap-3 lg:grid-cols-2">{data.rewardRuns.length?data.rewardRuns.map(run=><details key={run.id} className="rounded-xl border p-4"><summary className="flex cursor-pointer justify-between gap-3"><b className="capitalize">{run.reward_type} · {run.period_key}</b><b className="text-primary">{Number(run.total_np).toLocaleString()} NP</b></summary><p className="mt-2 text-xs text-muted-foreground">{run.eligible_count} eligible · {run.excluded_count} excluded · {date(run.executed_at)}</p><ol className="mt-3 text-sm">{run.recipients.map(item=><li key={item.userId}>#{item.place} · {Number(item.score).toLocaleString()} XP · {item.npAmount} NP</li>)}</ol></details>):<Empty text="No finalized rewards yet."/>}</div></section></div>}

    {tab==="rules"&&<div className="space-y-5"><section className={card}><h2 className="text-lg font-bold">NP & XP rules</h2><p className="text-sm text-muted-foreground">Version {data.config.version}. Saved changes apply immediately to future activity; history remains unchanged.</p></section><div className="grid gap-4 xl:grid-cols-2"><RuleGroups title="NP rules" value={npConfig} set={setNPConfig} omit={["store","weeklyGoals"]}/><RuleGroups title="XP rules" value={xpConfig} set={setXPConfig} omit={["weeklyGoal"]}/></div>{canReset&&<section className={card}><label className="text-sm font-bold">Reason<input value={configReason} onChange={event=>setConfigReason(event.target.value)} placeholder="Why are these rules changing?" className={`${control} mt-2 w-full`}/></label><button onClick={async()=>{const result=await request({action:"preview_config",npConfig,xpConfig},"config");if(result)setConfigPreview(result.changes)}} className={`${button} mt-4 border border-primary/30 text-primary`}>Preview changes</button>{configPreview.length>0&&<div className="mt-4 rounded-xl bg-muted/40 p-4"><p className="font-bold">{configPreview.length} changes</p><ul className="mt-2 max-h-52 overflow-auto text-xs">{configPreview.map(change=><li key={change.path}><b>{change.path}</b>: {String(change.before)} → {String(change.after)}</li>)}</ul><input value={configConfirmation} onChange={event=>setConfigConfirmation(event.target.value)} placeholder="Type ACTIVATE RULES" className={`${control} mt-4 w-full`}/><button disabled={configConfirmation!=="ACTIVATE RULES"||configReason.trim().length<3} onClick={async()=>{if(await request({action:"activate_config",npConfig,xpConfig,reason:configReason,confirmation:configConfirmation},"activate-config")){setConfigPreview([]);setConfigReason("");setConfigConfirmation("");setNotice("New NP and XP rules are active.")}}} className={`${button} mt-3 bg-primary text-primary-foreground`}>Activate now</button></div>}</section>}</div>}

    {tab==="gifts"&&<div className="grid gap-5 xl:grid-cols-[.9fr_1.1fr]"><section className={card}><h2 className="text-lg font-bold">Gift NP</h2><p className="text-sm text-muted-foreground">One row per learner: index number, NP amount, reason.</p><label className={`${button} mt-4 inline-flex cursor-pointer border`}>Upload CSV<input type="file" accept=".csv,text/csv" className="sr-only" onChange={async event=>{const file=event.target.files?.[0];if(file){setGiftText(await file.text());setGiftPreview(null)}event.target.value=""}}/></label><textarea value={giftText} onChange={event=>{setGiftText(event.target.value);setGiftPreview(null)}} rows={9} placeholder={"SM/SMS/22/0102,500,Challenge winner\nSM/SMS/22/0144,250,Participation award"} className={`${control} mt-3 w-full py-3 font-mono`}/><div className="mt-3 flex gap-2"><button disabled={!gifts.length} onClick={async()=>{const result=await request({action:"validate_gifts",gifts},"gifts");if(result)setGiftPreview(result)}} className={`${button} border`}>Validate recipients</button>{canReset&&giftPreview&&Boolean(giftPreview.valid)&&<button onClick={async()=>{if(await request({action:"gift_np",gifts},"send-gifts")){setGiftText("");setGiftPreview(null);setNotice("NP gifts sent and recorded.")}}} className={`${button} bg-primary text-primary-foreground`}>Send {Number(giftPreview.totalNP??0).toLocaleString()} NP</button>}</div>{giftPreview&&<GiftPreview value={giftPreview}/>}</section><section className={card}><h2 className="text-lg font-bold">Recent NP gifts</h2><div className="mt-4 space-y-2">{data.gifts.length?data.gifts.map(gift=><div key={gift.id} className="rounded-xl border p-3"><div className="flex justify-between"><div><p className="text-sm font-bold">{gift.name??gift.index_number}</p><p className="text-xs text-muted-foreground">{gift.index_number} · {gift.reason}</p></div><b className="text-emerald-600">+{gift.amount.toLocaleString()} NP</b></div><p className="mt-2 text-[11px] text-muted-foreground">{date(gift.created_at)}{gift.batch_id?" · Bulk gift":""}</p></div>):<Empty text="No NP gifts yet."/>}</div></section></div>}

    {creating&&<Modal title="Plan economy season" close={()=>setCreating(false)}><div className="grid gap-3 sm:grid-cols-2"><Field label="Season name" value={name} set={setName}/><Field label="Economy version" value={version} set={setVersion}/><Num label="Opening NP grant" value={openingGrant} set={setOpeningGrant}/><Num label="Minimum eligible questions" value={minimum} set={setMinimum}/><label className="text-xs font-bold sm:col-span-2">Start date<input type="datetime-local" value={startsAt} onChange={event=>setStartsAt(event.target.value)} className={`${control} mt-1 w-full`}/></label><div className="sm:col-span-2"><RewardTable title="Monthly NP rewards" value={monthly} set={setMonthly}/></div><div className="sm:col-span-2"><RewardTable title="Seasonal NP rewards" value={seasonal} set={setSeasonal}/></div></div><button disabled={busy==="create"||name.trim().length<3||!version.trim()} onClick={async()=>{if(await request({action:"create",name,economyVersion:version,openingGrant,minimumEligibleQuestions:minimum,monthlyRewards:monthly,seasonalRewards:seasonal,startsAt:startsAt||new Date().toISOString()},"create")){setCreating(false);setNotice("Season planned.")}}} className={`${button} mt-5 w-full bg-primary text-primary-foreground`}>Create season</button></Modal>}
    {editing&&<Modal title="Edit planned season" close={()=>setEditing(null)}><div className="grid gap-3"><Field label="Season name" value={editing.name} set={value=>setEditing({...editing,name:value})}/><Num label="Opening NP grant" value={editing.opening_grant} set={value=>setEditing({...editing,opening_grant:value})}/><label className="text-xs font-bold">Start date<input type="datetime-local" value={new Date(editing.starts_at).toISOString().slice(0,16)} onChange={event=>setEditing({...editing,starts_at:new Date(event.target.value).toISOString()})} className={`${control} mt-1 w-full`}/></label></div><button disabled={editing.name.trim().length<3||busy==="edit"} onClick={async()=>{if(await request({action:"edit_season",seasonId:editing.id,name:editing.name,openingGrant:editing.opening_grant,startsAt:editing.starts_at},"edit")){setEditing(null);setNotice("Planned season updated.")}}} className={`${button} mt-5 w-full bg-primary text-primary-foreground`}>Save season</button></Modal>}
    {activating&&<Modal title={`Start ${activating.name}`} close={()=>setActivating(null)}><p className="text-sm text-muted-foreground">Seasonal rewards will finalize. NP, purchases, cosmetics, and lifetime XP remain.</p>{impact?<dl className="mt-4 grid grid-cols-2 gap-3 rounded-xl bg-muted/40 p-4"><Stat label="Learners" value={impact.affectedLearners}/><Stat label="NP carried" value={impact.carriedNP?.toLocaleString()}/><Stat label="Opening NP total" value={impact.openingGrantTotal?.toLocaleString()}/><Stat label="Per learner" value={`${impact.openingGrantPerLearner} NP`}/></dl>:<p className="mt-4">Loading impact preview…</p>}<input value={confirmation} onChange={event=>setConfirmation(event.target.value)} placeholder={`Type START ${activating.name.toUpperCase()}`} className={`${control} mt-4 w-full`}/><button disabled={!impact||confirmation!==`START ${activating.name.toUpperCase()}`} onClick={async()=>{if(await request({action:"activate",seasonId:activating.id,confirmation},"activate")){setActivating(null);setNotice("New season started; NP balances were preserved.")}}} className={`${button} mt-3 w-full bg-destructive text-destructive-foreground`}>Start season</button></Modal>}
  </main>
}

function Stat({label,value}:{label:string;value:string|number|undefined}){return <div><dt className="text-[11px] text-muted-foreground">{label}</dt><dd className="font-bold tabular-nums">{value??"—"}</dd></div>}
function Summary({title,text,action}:{title:string;text:string;action:()=>void}){return <button onClick={action} className={`${card} text-left hover:border-primary/30`}><b>{title}</b><p className="mt-1 text-sm text-muted-foreground">{text}</p><span className="mt-4 block text-xs font-bold text-primary">Open →</span></button>}
function Empty({text}:{text:string}){return <p className="rounded-xl bg-muted/40 p-4 text-sm text-muted-foreground">{text}</p>}
function date(value:string){return new Date(value).toLocaleDateString(undefined,{year:"numeric",month:"short",day:"numeric"})}
function Modal({title,close,children}:{title:string;close:()=>void;children:ReactNode}){return <div className="fixed inset-0 z-[100] grid place-items-center overflow-y-auto bg-black/55 p-4"><div role="dialog" aria-modal="true" aria-label={title} className="my-6 w-full max-w-2xl rounded-3xl border bg-card p-6 shadow-2xl"><div className="flex justify-between"><h2 className="text-xl font-bold">{title}</h2><button onClick={close} aria-label="Close"><X/></button></div><div className="mt-5">{children}</div></div></div>}
function Field({label,value,set}:{label:string;value:string;set:(value:string)=>void}){return <label className="text-xs font-bold">{label}<input value={value} onChange={event=>set(event.target.value)} className={`${control} mt-1 w-full`}/></label>}
function Num({label,value,set}:{label:string;value:number;set:(value:number)=>void}){return <label className="text-xs font-bold">{label}<input type="number" min={0} value={value} onChange={event=>set(Number(event.target.value))} className={`${control} mt-1 w-full`}/></label>}
function RewardTable({title,value,set}:{title:string;value:number[];set:(value:number[])=>void}){return <div><div className="flex justify-between"><b className="text-xs">{title}</b><button type="button" onClick={()=>set([...value,0])} className="text-xs font-bold text-primary">+ Add place</button></div><div className="mt-2 grid gap-2">{value.map((amount,index)=><div key={index} className="flex items-center gap-2"><span className="w-16 text-xs">Place {index+1}</span><input type="number" min={0} value={amount} onChange={event=>set(value.map((item,i)=>i===index?Number(event.target.value):item))} className={`${control} flex-1`}/><span className="text-xs font-bold">NP</span>{value.length>1&&<button type="button" onClick={()=>set(value.filter((_,i)=>i!==index))}><X size={15}/></button>}</div>)}</div></div>}
function RuleGroups({title,value,set,omit}:{title:string;value:Record<string,unknown>;set:(value:Record<string,unknown>)=>void;omit:string[]}){return <section className={card}><h2 className="text-lg font-bold">{title}</h2><div className="mt-3 space-y-2">{Object.entries(value).filter(([key])=>!omit.includes(key)).map(([key,item])=><ConfigGroup key={key} name={key} value={item} set={next=>set({...value,[key]:next})}/>)}</div></section>}
function ConfigGroup({name,value,set}:{name:string;value:unknown;set:(value:unknown)=>void}){const [text,setText]=useState(()=>JSON.stringify(value,null,2)),[invalid,setInvalid]=useState(false);return <details className="rounded-xl border p-3"><summary className="cursor-pointer text-sm font-bold">{name.replace(/([a-z])([A-Z])/g,"$1 $2")}</summary><textarea value={text} onChange={event=>setText(event.target.value)} onBlur={()=>{try{set(JSON.parse(text));setInvalid(false)}catch{setInvalid(true)}}} rows={Math.min(12,Math.max(3,text.split("\n").length))} className={`${control} mt-3 w-full py-3 font-mono text-xs ${invalid?"border-destructive":""}`}/>{invalid&&<p className="text-xs text-destructive">Correct the JSON before previewing.</p>}</details>}
function GiftPreview({value}:{value:Record<string,unknown>}){const errors=Array.isArray(value.errors)?value.errors as Array<{row:number;error:string}>:[];return <div className={`mt-4 rounded-xl p-4 ${value.valid?"bg-emerald-500/10":"bg-destructive/10"}`}><b>{value.valid?`Ready to send ${Number(value.totalNP??0).toLocaleString()} NP`:"Correct these rows"}</b>{errors.map(item=><p key={`${item.row}-${item.error}`} className="text-xs text-destructive">Row {item.row}: {item.error}</p>)}</div>}
