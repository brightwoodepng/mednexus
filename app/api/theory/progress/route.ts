import { NextRequest,NextResponse } from "next/server"; import { requireTheoryCaller } from "@/lib/theory-auth"; import { getTheoryPool } from "@/lib/theory-api"
export async function POST(req:NextRequest){const c=requireTheoryCaller(req);if(c instanceof NextResponse)return c;const b=await req.json();if(typeof b.questionId!=="string"||!b.questionId)return NextResponse.json({error:"questionId is required"},{status:400});const p=await getTheoryPool();if(!p)return NextResponse.json({error:"No database configured"},{status:503});if(b.completed===false){await p.query("DELETE FROM mednexus_theory_completion_progress WHERE user_id=$1 AND question_id=$2",[c.uid,b.questionId])}else await p.query(`INSERT INTO mednexus_theory_completion_progress(user_id,collection_id,discipline_id,set_id,question_id) SELECT $1,collection_id,discipline_id,set_id,id FROM mednexus_theory_questions WHERE id=$2 ON CONFLICT(user_id,question_id) DO UPDATE SET completed_at=NOW()`,[c.uid,b.questionId]);return NextResponse.json({success:true})}
