import { NextRequest,NextResponse } from "next/server"; import { requireTheoryAdmin } from "@/lib/theory-auth"; import { getTheoryPool } from "@/lib/theory-api"
export async function POST(req:NextRequest){const d=requireTheoryAdmin(req);if(d)return d;const b=await req.json();if(typeof b.id!=="string"||typeof b.archived!=="boolean")return NextResponse.json({error:"id and archived are required"},{status:400});const p=await getTheoryPool();if(!p)return NextResponse.json({error:"No database configured"},{status:503});await p.query("UPDATE mednexus_theory_questions SET is_archived=$1,updated_at=NOW() WHERE id=$2",[b.archived,b.id]);return NextResponse.json({success:true})}
