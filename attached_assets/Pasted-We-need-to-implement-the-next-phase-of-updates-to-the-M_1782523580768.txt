We need to implement the next phase of updates to the MedNexus application, focusing heavily on authentication, secure user management. Please review the following requirements and implement them step-by-step.
1. Authentication, Login & Landing Page UI
Roles: Implement a role-based access system on the landing page with three distinct roles: Guest, User, and Admin.
Admin: Require a specific admin password to access the dashboard.
Guest (In-App): Allow guests to use the app and save their progress locally or in a temporary session state. This data must be completely wiped only when they explicitly click "Log Out" or the session ends.
User (Registration & Login): Require standard login. User data must be saved to a persistent database so they can continue where they left off across different sessions. Update the registration form to capture: Name, Level, Index Number, and Password.
Forgot Password: On the login screen, if a user forgets their password, they should be prompted to contact the admin.
Footer/Support UI: On the landing page, display the text "Created by Britechinc". Below or next to it, display "For support contact admin" accompanied by a WhatsApp icon. The WhatsApp icon should be a clickable link directing to [https://wa.me/233543982307](https://wa.me/233543982307), but the phone number itself should remain hidden from the visible UI text.
2. Auto-Validation Logic for User Registration
Index Number Input: Users must be able to input their Index Number with or without slashes (e.g., they can type smsms220092 or sm/sms/22/0092). Format it correctly in the backend.
Auto-Approval: Check the index number against this specific pattern: sm/sms/YY/NNNN or sm/gem/YY/NNNN (where YY is any 2-digit year, and NNNN is a 4-digit sequence number). If the input matches this pattern (ignoring slashes), automatically set the account status to "Approved".
Manual Approval (Fallback): If the inputted index number does not match the format above, create the account but set their status to "Pending Approval". Pending users should not have full app access until an admin approves them.
3. Database & Admin Dashboard (Strictly Admin-Only User Management)
Ensure a persistent user database is properly connected.
Access Control: The entire User Management view and all actions within it must be strictly protected and accessible ONLY by the Admin role.
Dashboard Overview: Display the total number of registered users.
Pending Approvals Queue: Allow the admin to review user details (Name, Level, Index Number) for those who failed auto-validation, and click "Approve" or "Reject".
Search & Filter: Add a search bar to look up users by their name or index number. Include filters to sort and filter users by their Date of Account Opening, Name, and Status (Approved/Pending).
Account Actions:
Add the ability to Delete specific user accounts.
Add a "Reset Password" button for each user. Clicking this generates a One-Time Password (OTP) on the admin's screen to share with the user.
Forced Password Update: If a user logs in using an OTP generated by the admin, the system must immediately redirect them to their profile and force them to update their password before they can access the rest of the application.